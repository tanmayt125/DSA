#include<bits/stdc++.h>

using namespace std;

int main(){
    // std::cout operates entirely in user space, managing the data and buffering it before making system calls to the kernel.
    // The kernel handles the actual writing of the data to the console or other output devices, 
    // but the high-level operations of std::cout are within the domain of user space.
    cout<<"Hello World";
}